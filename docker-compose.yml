version: '3.8'
services:
  wazuh-dojo-integrator:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./config.yaml:/app/config.yaml:ro
    env_file:
      - .env
    environment:
      - ASSIGNMENT_DB_PATH=/data/assignments.sqlite